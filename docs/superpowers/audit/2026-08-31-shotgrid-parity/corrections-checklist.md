# Corrections Verification Checklist

**Result:** 27/28 Present, 1/28 Partial, 0/28 Missing/Regressed.

Checked against the current worktree state (`worktree-shotgrid-parity-audit`,
descended from `main` at `a862b9f` — commits `77a640f` and `a862b9f` are
confirmed present in this branch's history via `git log`).

| # | Claim | Verdict | Evidence (file:line) | Notes |
|---|-------|---------|----------------------|-------|
| 1 | `nginx.conf` — `/api/` `proxy_pass` block added | ✅ Present | `artifacts/forge/nginx.conf:9-16` | `location /api/ { proxy_pass http://api:3001/api/; ... }` present with forwarding headers and `proxy_cookie_path`. |
| 2 | `docker-compose.yml` — `api` migration command fixed (no broken `pnpm --filter` against pruned image) | ✅ Present | `docker-compose.yml:63-64` | `command:` invokes `./node_modules/.bin/tsx src/migrate.ts` directly, bypassing pnpm workspace resolution; comment block (lines 54-62) explains why. |
| 3 | `artifacts/api-server/Dockerfile` — conditional migration regen, placeholder `DATABASE_URL` at build, `@workspace/db` deployed without `--prod` | ✅ Present | `artifacts/api-server/Dockerfile:49-52,67` | `RUN if [ ! -d "lib/db/drizzle" ]; then DATABASE_URL="postgresql://placeholder..." pnpm --filter "@workspace/db" run generate; fi` (49-52); `RUN pnpm --filter "@workspace/db" deploy --legacy /prod/db` has no `--prod` flag (67), vs. api-server/api-zod deploys which do use `--prod` (66, 68). |
| 4 | `routes/index.ts` — `usersRouter` mounted; `/api/healthz` not double-mounted | ✅ Present | `artifacts/api-server/src/routes/index.ts:6,14,18` | `usersRouter` imported and mounted at `/users` (line 18); `healthRouter` mounted at `"/"` (not `"/healthz"`) with a comment explaining the double-mount would otherwise occur. |
| 5 | `routes/users.ts` — tenant-scoped, strips `hashedPassword`, hashes new passwords with argon2 | ✅ Present | `artifacts/api-server/src/routes/users.ts:56-59,125,140,210`; `artifacts/api-server/src/lib/auth.ts:1,14-15` | `GET /` filters `eq(usersTable.tenantId, tenantId)`; `POST /` and `PATCH /:id` destructure `hashedPassword` out via `const { hashedPassword: _omit, ...user }` before responding; `hashPassword()` calls `argon2.hash(password)`. |
| 6 | `railway.json` — `healthcheckPath` is `/api/healthz` | ✅ Present | `railway.json:8` | `"healthcheckPath": "/api/healthz"`. |
| 7 | `useUsers.ts` — no `/api/api/users` double-prefix; unwraps bare-array shape | ✅ Present | `artifacts/forge/src/hooks/useUsers.ts:19-25`; `artifacts/forge/src/lib/apiClient.ts:20-25` | `useUsers()` calls `apiFetch<UserDTO[]>("/users")` and returns it directly (no `.users` unwrap); `apiClient.ts`'s `apiFetch` prepends `/api` exactly once when `VITE_API_URL` is unset (no `/api` prefix already present check via `endpoint.startsWith("/api")`). |
| 8 | `scripts/package.json` — has a `"seed"` script | ✅ Present | `scripts/package.json:8` | `"seed": "tsx ./src/seed.ts"`. |
| 9 | `lib/db/seed.cjs` — deleted | ✅ Present | `lib/db/seed.cjs` (absent) | File does not exist on disk (`ls` returns "No such file or directory"). |
| 10 | `artifacts/forge/Dockerfile` — `COPY nginx.conf` resolves against build context | ✅ Present | `artifacts/forge/Dockerfile:38` | `COPY artifacts/forge/nginx.conf /etc/nginx/conf.d/default.conf` — full repo-relative path, matching `docker-compose.yml`'s `context: .` for this service. |
| 11 | `lib/db/src/migrate.ts` — guards "object already exists" errors without swallowing genuine failures | ✅ Present | `lib/db/src/migrate.ts:34-38,46-56,66-87` | `ALREADY_EXISTS_SQLSTATES` set (42P07/42710/42701) checked via `isAlreadyExistsError()` which walks the `.cause` chain; only matching codes are warned-and-`process.exit(0)`, `if (!isAlreadyExistsError(err)) throw err;` re-throws everything else. |
| 12 | `lib/db/src/schema/core.ts` — `users.email` UNIQUE; `users.role_id` has real FK to `tenant_roles(id)` | ✅ Present | `lib/db/src/schema/core.ts:27-29,38` | `roleId: text("role_id").notNull().references(() => tenantRolesTable.id, { onDelete: "cascade" })`; `email: text("email").notNull().unique()`. |
| 13 | `routes/users.ts` — `POST /api/users` validates `roleId` belongs to caller's tenant (400), checks duplicate email (409) | ✅ Present | `artifacts/api-server/src/routes/users.ts:83-95,116-123` | Role lookup scoped by `and(eq(tenantRolesTable.id, roleId), eq(tenantRolesTable.tenantId, tenantId))`, returns 400 if not found; existing-email lookup returns `409 { error: "Email already in use" }`. |
| 14 | `lib/api-spec/openapi.yaml` + `lib/api-zod` + `lib/api-client-react` — `GET`/`POST /users` documented and codegen regenerated/committed | ✅ Present | `lib/api-spec/openapi.yaml:165-215`; `lib/api-zod/src/generated/types/user.ts`, `createUserBody.ts`; `lib/api-client-react/src/generated/api.ts:694-761` | OpenAPI spec has `/users` `get`/`post` operations (`getUsers`/`createUser`); api-zod has generated `User`/`CreateUserBody` types; api-client-react's generated `api.ts` has `getUsers`, `useGetUsers`, `getCreateUserUrl`/`createUser` hooks. |
| 15 | `docker-compose.yml` — `api` internal port fixed at 3001 matching nginx; `db` healthcheck uses `pg_isready -h 127.0.0.1` with `start_period` | ✅ Present | `docker-compose.yml:16-30,42-53` | `db.healthcheck.test` is `pg_isready -h 127.0.0.1 -U ... -d ...` with `start_period: 10s`; `api` service comment (42-46) states the container's internal port is fixed at 3001 via the Dockerfile's `ENV PORT=3001`, matching `nginx.conf`'s `proxy_pass http://api:3001/api/`. |
| 16 | `artifacts/*/Dockerfile` — uses `corepack enable` (not `corepack prepare pnpm@latest --activate`) | ✅ Present | `artifacts/api-server/Dockerfile:16`; `artifacts/forge/Dockerfile:20` | Both: `RUN corepack enable && corepack install` — no `corepack prepare pnpm@latest --activate` anywhere in either Dockerfile. |
| 17 | `artifacts/api-server/.env.example` — exists with placeholder values | ✅ Present | `artifacts/api-server/.env.example:1-24` | File exists; `DATABASE_URL="postgresql://forge_dev:replace_me@localhost:5432/forge_dev"` and other clearly-placeholder values, with a comment "NEVER commit real credentials." |
| 18 | `forge-final.md` — the live Neon password once quoted in §3 is redacted | ✅ Present | `forge-final.md:58` | `DATABASE_URL="postgresql://neondb_owner:npg_****REDACTED****@ep-lingering-bar-azva7gtp-pooler.c-3.ap-southeast-1.aws.neon.tech/neondb?..."` — password segment is `****REDACTED****`, not verbatim. |
| 19 | `middleware/rbac.ts`'s `requireCapability` queries `tenantRoleCapabilitiesTable` (not just `req.roleId` truthy), applied to `POST /api/users` (not `GET /`) with capability id `"manage_members"` | ✅ Present | `artifacts/api-server/src/middleware/rbac.ts:6-29`; `artifacts/api-server/src/routes/users.ts:19,68` | `requireCapability()` does a real `db.select().from(tenantRoleCapabilitiesTable).where(and(eq(...roleId, req.roleId), eq(...capabilityId, capabilityId)))` lookup and 403s if no `grant` row; `router.get("/", ...)` (line 19) has no `requireCapability` call, while `router.post("/", requireCapability("manage_members"), ...)` (line 68) does. |
| 20 | `docker-compose.yml`'s `db` has `pg_isready` healthcheck, `api`'s `depends_on` uses `condition: service_healthy` long form; root `package.json` has `"packageManager": "pnpm@<version>"` | ✅ Present | `docker-compose.yml:22-30,38-40`; `package.json:5` | `db.healthcheck.test` uses `pg_isready` (22-26); `api.depends_on.db.condition: service_healthy` (38-40) is the long form, not a bare service-name list; root `package.json` has `"packageManager": "pnpm@11.20.0"`. |
| 21 | `routes/users.ts` — `GET /api/users` left-joins `tenant_roles` so `role` is a real name; blocks the `client` role from this endpoint | ⚠️ Partial | `artifacts/api-server/src/routes/users.ts:37-60` | The left-join part is present and correct: `.leftJoin(tenantRolesTable, eq(usersTable.roleId, tenantRolesTable.id))` with `role: tenantRolesTable.name` in the select (lines 42,52). However, the endpoint does **not** block the `client` role — it returns `200` with a scoped subset (`STUDIO_LEADERSHIP_ROLES` only) instead of a `403`/block. This is not a regression: it is the intentional, later revision documented by item 28 (commit `a862b9f`), which explicitly superseded the "block outright" behavior with query-scoping. Item 21 as literally worded (describing the state after `77a640f` alone) no longer matches current `main`/this branch; item 28's description is what's actually in the code today. |
| 22 | `scripts/src/seed.ts` — per-role `tenant_roles` re-query scoped by `(name, tenantId)`, not name alone | ✅ Present | `scripts/src/seed.ts:197-205` | `.where(and(eq(tenantRolesTable.name, roleDef.name), eq(tenantRolesTable.tenantId, tenantId)))` — scoped by both fields, with a comment explaining the cross-tenant risk of scoping by name alone. |
| 23 | Frontend `auth.ts` store — department rows normalized to mock `Department` shape's field names at hydration | ✅ Present | `artifacts/forge/src/store/auth.ts:76-96` | `normalizedDeps` maps API fields to mock fields: `abbreviation: d.abbr`, `studioId: d.tenantId`, `description: `${d.pipeline} pipeline``, plus derived `supervisorId`/`leadId` — matches the described `abbr`/`pipeline`/`tenantId` → `abbreviation`/`description`/`studioId` translation. |
| 24 | `departments.tsx` — persisted "Pipeline Flow" order self-heals against the live department list | ✅ Present | `artifacts/forge/src/pages/departments.tsx:44-61` | `useEffect` checks `resolvedCount === 0` (no persisted ids resolve against live `DEPARTMENTS`) and if so rebuilds `pipelineOrder` from `DEPARTMENTS` sorted by `pipelineOrder`, then calls `setPipelineOrder(liveOrder)`. |
| 25 | `useUsers.ts` / `admin.tsx` — uses the real `UserDTO` the endpoint returns, no `any` cast | ✅ Present | `artifacts/forge/src/hooks/useUsers.ts:5-25`; `artifacts/forge/src/pages/admin.tsx:3,55` | `useUsers()` is typed `useQuery<UserDTO[]>` with a concrete `UserDTO` interface (id/tenantId/roleId/role/departmentId/email/name/title/avatar/status/createdAt); `admin.tsx` imports and calls `useUsers()` directly with no `as any`/`: any` cast on the returned data (the only `: any` in the file is on unrelated `catch (err: any)` blocks at lines 90/109). |
| 26 | `artifacts/forge/src/scripts/generateMockData.ts` — deleted | ✅ Present | `artifacts/forge/src/scripts/` (directory absent) | `find`/`ls` on `artifacts/forge/src/scripts` returns "No such file or directory" — the whole directory, including `generateMockData.ts`, is gone. |
| 27 | `departments.tsx` — Pipeline Flow self-heal fallback filters to `pipelineOrder > 0`, excluding "Production Management" | ✅ Present | `artifacts/forge/src/pages/departments.tsx:53-59` | `.filter((d) => d.pipelineOrder > 0).sort((a, b) => a.pipelineOrder - b.pipelineOrder)` inside the self-heal block, with an inline comment: "Production Management (pipelineOrder 0) is studio overhead, not a pipeline stage, and was never meant to appear in this strip." |
| 28 | `routes/users.ts` — `GET /api/users` scopes the query for the `client` role (200 with admin/production_head rows) rather than blocking outright | ✅ Present | `artifacts/api-server/src/routes/users.ts:9,31-60` | `STUDIO_LEADERSHIP_ROLES = ["admin", "production_head"]` (line 9); when `isClient` is true, the `where` clause becomes `and(eq(usersTable.tenantId, tenantId), inArray(tenantRolesTable.name, STUDIO_LEADERSHIP_ROLES))` (lines 53-59) and the handler still returns `res.json(users)` (200), never a block/403 for this role. |
